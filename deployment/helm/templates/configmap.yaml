apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dataspace-consumer-framework.fullname" . }}-config
  labels:
    {{- include "dataspace-consumer-framework.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: |
    {{- $value | nindent 4 }}
  {{- end }}
