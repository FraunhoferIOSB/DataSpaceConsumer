apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataspace-consumer-framework.fullname" . }}
  labels:
    {{- include "dataspace-consumer-framework.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dataspace-consumer-framework.selectorLabels" . | nindent 4 }}
