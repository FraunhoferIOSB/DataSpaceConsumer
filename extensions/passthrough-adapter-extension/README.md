# Passthrough Adapter Extension

This extension passes the converter response through and returns it. Used for synchronous requests.

## Configuration

```yaml
adapter:
  implementation: de.fraunhofer.iosb.ilt.dataspace_consumer.passthrough_adapter_extension.AdapterImpl
```
