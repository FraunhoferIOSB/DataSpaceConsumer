# Console Adapter Extension

This extension logs the retrieved data to the console output for testing and debugging purposes.

## Configuration

```yaml
adapter:
  implementation: de.fraunhofer.iosb.ilt.dataspace_consumer.console_adapter_extension.ConsoleAdapter
  config: {}  # No additional configuration required
```

Retrieved and converted data is printed to standard output for initial verification.
